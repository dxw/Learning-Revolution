=============================================================================
THE LEARNING REVOLUTION
=============================================================================

You are watching <%= filter_core_description(@email_subscription.filter, false) %>,
and new events have been added:

<% @events.each do |event| -%>
-----------------------------------------------------------------------------
<%= event.title %>
<% if event.end -%>
<%= event.start.strftime("%e %B %R%p") %> until<%=
(event.start.strftime('%Y-%M-%d') == event.end.strftime('%Y-%M-%d')) ? '' : event.end.strftime(' %e %B') %><%=
event.end.strftime(" %R%p") %>
<% else -%>
<%= event.start.strftime("%e %B %R%p") %>
<% end -%>
<% if event.venue -%>
<%= event.venue.name %>
<% end -%>

More details:
<%= url_for_event(event, :only_path => false) %>
<% end -%>
-----------------------------------------------------------------------------

To unsubscribe from these email updates, go here:
<%= unsubscribe_email_subscription_url(:id => @email_subscription, :secret => @email_subscription.secret, :only_path => false) %>

http://learningrevolution.direct.gov.uk/

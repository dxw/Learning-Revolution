-----------------------------------------------------------------------------
<%= event.title %>
<% if event.end -%>
<%= event.start.strftime("%e %B %R%p") %> until<%=
(event.start.strftime('%Y-%M-%d') == event.end.strftime('%Y-%M-%d')) ? '' : event.end.strftime(' %e %B') %><%=
event.end.strftime(" %R%p") %>
<% else -%>
<%= event.start.strftime("%e %B %R%p") %>
<% end -%>
<% if event.venue -%>
<%= event.venue.name %>
<% end -%>

More details:
<%= url_for_event(event, :only_path => false) %>
